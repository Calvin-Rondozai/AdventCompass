EXPO_PUBLIC_GROQ_API_KEY=
# Optional fallback keys — used in order if an earlier one hits a Groq rate limit/quota
# (429) or is invalid/revoked (401). Leave blank/unset if you only have one key.
EXPO_PUBLIC_GROQ_API_KEY_2= 
EXPO_PUBLIC_GROQ_API_KEY_3=
EXPO_PUBLIC_GROQ_API_KEY_4=
